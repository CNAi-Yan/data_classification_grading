# 主配置文件
spring:
  # 应用名称
  application:
    name: sensitive-data-detector
  
  # 配置中心设置 - 默认禁用，使用本地配置
  cloud:
    config:
      enabled: false
      import: "optional:configserver:"
      import-check:
        enabled: false

# 激活的配置文件 - 默认使用开发环境
spring.profiles.active: dev
